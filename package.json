{
  "name": "ai-interview-coach-root",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "build": "npm run build --prefix client",
    "install:client": "npm install --prefix client"
  }
}
